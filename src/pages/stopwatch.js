import React from 'react';

const Stopwatch = () => (
    <div className="title">
        <h4>Stopwatch</h4>
        <p>This is stopwatch with some functions</p>
    </div>
);

export default Stopwatch;